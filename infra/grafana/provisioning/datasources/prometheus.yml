apiVersion: 1

datasources:
  - name: Prometheus
    # Fixed uid referenced by the provisioned dashboard JSONs — do not change independently.
    uid: dfrh2ocbcem80b
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
